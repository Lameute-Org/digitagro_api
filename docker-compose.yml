version: "3.9"

services:
  django_app:
    build: .
    container_name: digitagro_api
    ports:
      - "8001:8000"   # Port public VPS 8001 → interne 8000
    volumes:
      - .:/app
    restart: always
